extends: default
ignore: |
  .venv/
  node_modules/
  tests/integration/adapters/cassettes/
rules:
  line-length:
    max: 200
  truthy:
    check-keys: false
  document-start: disable
  indentation: disable
