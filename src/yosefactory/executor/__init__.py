"""One shape for driving a vendor agent, and the one lane that has a credential today.

D021: thin wrappers implementing the same interface, so the core is executor-agnostic. This is the
only package in the repository allowed to know a vendor's name.
"""
