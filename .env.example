# Copy to .env (already gitignored -- never commit the real file) and fill in the value.
# Produced by `claude setup-token` (requires an active Claude subscription). D021 keeps usage
# credits off, so this is a subscription token, not a pay-as-you-go API key -- see
# openspec/changes/run-the-factory-in-a-container/design.md, D2.
CLAUDE_CODE_OAUTH_TOKEN=
