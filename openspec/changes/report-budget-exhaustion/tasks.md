# Tasks

- [x] 1. Measure what the binary reports when a budget is exhausted, against the real flag
- [x] 2. Classify budget exhaustion as its own outcome, from either field that names it
- [x] 3. Cover the healthy path, so the new branch cannot capture a completed run
- [x] 4. Record the larger question — which vocabulary is authoritative — as out of scope
